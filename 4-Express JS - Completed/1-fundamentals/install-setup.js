/*
  Express Install and Setup
  -------------------------
  Express is a web framework for Node.js.

  Create package.json:

    npm init -y

  Install Express:

    npm install express

  Optional development auto-restart tool:

    npm install -D nodemon

  Typical package.json scripts:

    "scripts": {
      "start": "node app.js",
      "dev": "nodemon app.js"
    }

  Express 5 note:
  This project uses Express 5 syntax and behavior. Most beginner code looks the
  same as Express 4, but Express 5 handles rejected promises from async route
  handlers more directly.
*/

console.log("Run npm install in the Express project folder before running apps.");
